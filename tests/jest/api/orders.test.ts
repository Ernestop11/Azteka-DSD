/**
 * Jest order API templates.
 */

describe('POST /api/orders', () => {
  it('creates order with metadata + events (template)', () => {
    expect(true).toBe(true)
  })

  it.todo('rejects invalid override pricing')
})

describe('GET /api/orders/history/[customerId]', () => {
  it('summarizes bundle matches and trendline (template)', () => {
    expect(true).toBe(true)
  })
})
